# API reference

This section documents the full public API of `obsidian-dev-utils`, generated from the library's TSDoc.

Each module below corresponds to an import subpath — for example, the `string` module is imported as
`obsidian-dev-utils/string`, and `obsidian/modals` as `obsidian-dev-utils/obsidian/modals`. Browse the
modules in the sidebar or the list below.

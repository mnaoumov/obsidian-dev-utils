/**
 * @fileoverview
 * This file ensures that the `localforage` module is imported and its types are available for use.
 * `localforage` is a JavaScript library that provides an easy-to-use API for offline storage, supporting
 * various backends like IndexedDB, WebSQL, and localStorage.
 */

import "localforage";
